## Звіт по виконаних завданнях фінального проєкту
**Завдання 1**
- Реалізовано **реверсування однозв’язного списку** зі зміною посилань між вузлами. Код виконується.
- Створено **функцію сортування** для однозв’язного списку. Код виконується.
- Реалізовано **об’єднання двох відсортованих однозв’язних списків** в один відсортований. Код виконується.

**Завдання 2**
- Програма **візуалізує фрактал “дерево Піфагора”**. Код виконується.
- Користувач може **вказати рівень рекурсії**.

**Завдання 3**
- Реалізовано **алгоритм Дейкстри** для пошуку найкоротших шляхів у графі з використанням **бінарної купи (піраміди)**.
- Створено граф, застосовано піраміду для оптимізації вибору вершин, виконано обчислення найкоротших шляхів **від початкової вершини до всіх інших**.

**Завдання 4**
- Реалізовано **візуалізацію бінарної купи**. Код виконується.

**Завдання 5**
- Реалізовано алгоритми **DFS** та **BFS** для візуалізації обходу дерева **в глибину** та **в ширину** (використано стек і чергу). Код виконується.
- **Кольори вузлів** змінюються від темніших до світліших відтінків залежно від порядку обходу.

**Завдання 6**
- Реалізовано функцію на основі **жадібного алгоритму**, що повертає **назви страв** і максимізує **співвідношення калорій до вартості** в межах заданого бюджету. Код виконується.
- Реалізовано функцію з **динамічним програмуванням (0/1 рюкзак)**, яка повертає **оптимальний набір страв** для **максимізації калорійності** при заданому бюджеті. Код виконується.

**Завдання 7**
- Змодельовано кидання **двох ігрових кубиків** методом **Монте-Карло** та побудовано **таблицю сум** і їх **ймовірностей**. Код виконується.
- Проведено велику кількість симуляцій, підраховано частоти появи кожної суми та **обчислено імовірності**.
- Побудовано **таблицю/графік** із отриманими ймовірностями.
- Зроблено **висновки щодо коректності**: результати Монте-Карло **зіставлено з аналітичними** розрахунками.



**Висновки (додано 2025-09-27 23:26:18)**

- Проведено 1,000,000 симуляцій кидання двох кубиків. Отримані методом Монте-Карло ймовірності добре узгоджуються з аналітичними розрахунками.
- Максимальна відносна похибка серед сум: 1.04%.
- Висновок: симуляція реалізована коректно; оцінки ймовірностей збігаються з аналітичними розрахунками в межах статистичної похибки.